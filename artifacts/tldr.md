# Artifact — TL;DR

Inherits `register-declarative.md`.

APPLIES WHEN:  the user explicitly asks you to shorten or summarize existing output.

RULES:
- Pick one body structure in this order: 3+ items sharing a common set of fields → table; else distinct topics → `#`/`##` headings; else → bullets.
- Compress only: add no claim or number the source didn't state.
- Carry every source number, owner, and date through the cut.

SHAPE (a lead line, then one body form from the body-structure rule):
```
**<one-line bottom line>**

— table, when 3+ items share fields:
| <field> | <field> | <field> |
|---------|---------|---------|

— headings, when topics are distinct:
## <topic>

— bullets, when points are independent:
- <point that survives the cut>
```

AVOID:
- Imposing a heading or table skeleton on content too small to fill it.
- Combining body forms when the body-structure rule selects one.
