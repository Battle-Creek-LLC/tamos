---
name: commit-message
description: Write a git commit subject and body. Use when asked to write a commit message, name a commit, describe a diff for the log, or when told to commit work that was just done.
---

## Step 0 — pull the rules

Read `${CLAUDE_PLUGIN_ROOT}/artifacts/commit-message.md` and apply its RULES, SHAPE, and AVOID.

## Source

Use the content in this turn — the text passed to the skill, what the user just
pasted or referenced, or the work just completed. If none exists, ask which
subject to use; don't guess.
